/**
 * App-shell footer.
 *
 * Intentionally empty for the redesign. The sample (`docs/aether-redesign.html`)
 * has no persistent footer — every useful link lives in the command palette
 * (⌘K) or the user dropdown in the header. Keeping this component as the
 * mount point so we can drop in a status / build-info strip later without
 * touching `__root.tsx`.
 */
export default function Footer() {
  return null;
}
