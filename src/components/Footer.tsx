// Empty mount point — useful links live in the command palette (⌘K) and
// the header user dropdown. Kept so a status/build-info strip can drop in
// later without touching `__root.tsx`.
export default function Footer() {
  return null;
}
